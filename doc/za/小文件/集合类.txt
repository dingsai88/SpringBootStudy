I.Collection(List有序set不重复) 、map
List Array、link、 CopyOnWriteArrayList
set底层hashset、linkedhashset、treeset
HashMap(16因0.75加2N)TreeMap LinkedHashMap
JDK8之前Hash+链表 ；8后红黑树 链表超过阈值
哈希碰撞拒绝服务、相同的哈希值，变链表CPU100
ConcurrentHash7分段锁 数组+链表
JDK8 Node + CAS + Synchronized设计
空node节点cas、有数node synchronized
Queue阻塞非阻塞、有界无界、单端双端
