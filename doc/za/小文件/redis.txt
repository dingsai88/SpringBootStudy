ziplist压缩双向链表：总字+最后+个数+()+zlend255
list:双链64字512个。hash散列+链64字512个
set不重:基于hash:整数 512有序数组
sortedset:跳表:64字128个
持久:RDB快照fork。AOF追加慢
主从：replication ID(复制ID):(记录发送数据的多少)
哨兵：高可用自动选主。分片：高性能 三倍
搜集（图广度）》分析（切词）>索引(倒排)>查询
普通(同、异、单向)、事务、定时、顺序消息